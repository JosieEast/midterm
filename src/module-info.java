module assignment6 {
    // Export the package containing your main code
    exports cis;

    // Specify the module dependencies
    requires org.junit.jupiter.api;
    requires org.junit.jupiter.engine;
    // Optionally, if you're using the JUnit Platform Console Standalone for running tests:
    // requires org.junit.platform.console;
}
